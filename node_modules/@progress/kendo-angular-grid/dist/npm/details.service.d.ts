import { EventEmitter } from '@angular/core';
/**
 * @hidden
 */
export declare class DetailsService {
    changes: EventEmitter<any>;
    private rowState;
    toggleRow(index: number, dataItem: any): void;
    isExpanded(index: number): boolean;
}
